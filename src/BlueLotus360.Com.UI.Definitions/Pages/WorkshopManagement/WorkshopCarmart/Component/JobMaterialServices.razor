@using BL10.CleanArchitecture.Domain.Entities.WorkShopManagement
@using BlueLotus360.CleanArchitecture.Domain.Entities.Order
@using Telerik.Blazor
<div class="tab-scroll">
    @if (validator!=null)
    {
        <ValidationPopUp ValidationMessages="validator.UserMessages.UserMessages" @ref="_refUserMessage" />
    }
    

    <div class="top-button-bar mt-3">
    <MudIconButton Icon="@Icons.TwoTone.ArrowBack"
                   Variant="Variant.Outlined"
                   Color="Color.Primary" Size="Size.Small" OnClick="OnBack" Disabled="@isBackButtonDisabled" />
</div>
    <div class="row" >

    <div id="job-material-service-section1" class="col-12 mt-3 mb-3">
        <div class="d-flex text-center justify-content-between align-items-baseline mb-3">
            <div></div>
            <div class="d-flex text-center justify-content-center align-items-baseline">
                    <h5> @($"Work Order({DataObject.OrderCategory1.CodeName ?? ""})-{DataObject.OrderPrefix.CodeName ?? "" + DataObject.OrderNumber ?? ""}") </h5>
            </div>
            <MudButton Variant="Variant.Filled" 
                        StartIcon="@Icons.Material.Filled.ArrowLeft" 
                       Color="Color.Primary" OnClick="gotoCreateNewWorkOrder" Disabled="@IsButtonDisabled" Class="mr-3">Create New WorkOrder</MudButton>
        </div>
        <VehicleSummaryTable SelectedVehicle="DataObject.SelectedVehicle" />
        
    </div>

    <div class="col-12 mt-3 mb-12">
        <MudTabs KeepPanelsAlive="true" 
                Position="Position.Top" 
                Rounded="true" Border="true" ApplyEffectsToContainer="true" Class="mat-ser-other-tab">

            <MudTabPanel Text="Material" >
                     <div class="text-center mt-3">
                        <h5>MATERIALS</h5>
                        <hr class="mt-2 mb-3" />
                    </div>
                    
                    <div class="d-flex">
                        <UIRenderere FromSection="@MaterialPopUp"
                                     DataObject="@DataObject"
                                     InteractionLogics="@InteractionLogic"
                                     ObjectHelpers="@ObjectHelpers" />
                    </div>

                    <div class="mt-3">
                        @if (DataObject.WorkOrderMaterials != null)
                        {
                            <TelerikGrid Data=@DataObject.WorkOrderMaterials.Where(x=>x.IsActive==1 && !x.TransactionItem.ItemName.Equals("-")).ToList()
                                     FilterMode="@GridFilterMode.FilterMenu"
                                     Pageable="true"
                                     PageSize="3"
                                     @ref=GridRef1>
                                <GridToolBar>
                                    <div class="d-flex align-items-center">
                                        <span class="font-weight-bold">Materials</span>
                                    </div>
                                </GridToolBar>
                                <GridAggregates>
                                    <GridAggregate Field="TransactionQuantity" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="DiscountAmount" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="PrincipleAmount" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="CompanyAmount" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="CustomerAmount" Aggregate="@GridAggregateType.Sum" />
                                    <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
                                </GridAggregates>
                                <GridColumns>
                                <GridColumn Width="100px">
                                    <Template>
                                        @{
                                            var ctx = context as OrderItem;
                                            @if (ctx != null)
                                            {
                                                <MudButtonGroup Color="Color.Primary" Variant="Variant.Text">
                                                    <MudIconButton Icon="@Icons.TwoTone.ModeEdit" OnClick="@(()=>MaterialEditHandler(ctx))" />
                                                    <MudIconButton Icon="@Icons.TwoTone.Delete" OnClick="@(()=>MaterialDeleteHandler(ctx))" />
                                                    
                                                </MudButtonGroup>
                                                
                                            }

                                        }
                                    </Template>


                                </GridColumn>
                                    @*<GridColumn Field="TransactionItem.ItemCode" Title="Id" Width="100px" />*@
                                    <GridColumn Field="TransactionItem.ItemName" Title="Material" Width="250px" />
                                    <GridColumn Field="TransactionUnit" Title="Unit" Width="100px" />
                                    <GridColumn Field="TransactionQuantity" Title="Qty" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                    <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                    <GridColumn Field="DiscountPercentage" Title="Dis%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                                    <GridColumn Field="DiscountAmount" Title="Dis Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>

                                    <GridColumn Field="PrinciplePrecentage" Title="Princ%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))" />
                                    <GridColumn Field="PrincipleAmount" Title="Princ Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                    <GridColumn Field="CompanyPrecentage" Title="CM%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))" />
                                    <GridColumn Field="CompanyAmount" Title="CM Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                    <GridColumn Field="CustomerAmount" Title="Cus Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                    <GridColumn Field="SubTotal" Title="Sub Total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                        <FooterTemplate>
                                            <div style="text-align:right;">
                                                <span>@context.Sum?.ToString("N2")</span>
                                            </div>

                                        </FooterTemplate>
                                    </GridColumn>
                                   
                                </GridColumns>
                            </TelerikGrid>
                        }

                    </div>
           
            </MudTabPanel>
            <MudTabPanel Text="Service" >

                <div class="text-center mt-3">
                    <h5 class="mt-3">SERVICES</h5>
                    <hr class="mt-2 mb-3" />
                </div>
                 <div class="d-flex">
                <UIRenderere FromSection="@ServicePopUp"
                             DataObject="@DataObject"
                             InteractionLogics="@InteractionLogic"
                             ObjectHelpers="@ObjectHelpers" />
            </div>
                <div>
                @if (DataObject.WorkOrderServices != null)
                {
                    <TelerikGrid Data=@DataObject.WorkOrderServices.Where(x=>x.IsActive==1 && !x.TransactionItem.ItemName.Equals("-")).ToList()
                             FilterMode="@GridFilterMode.FilterMenu"
                             PageSize="3"
                             Pageable="true"
                                     @ref="GridRef2">
                        <GridToolBar>
                            <div class="d-flex align-items-center">
                                <span class="font-weight-bold">Services</span>
                            </div>
                        </GridToolBar>
                        <GridAggregates>
                            <GridAggregate Field="TransactionQuantity" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="DiscountAmount" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="PrincipleAmount" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="CompanyAmount" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="CustomerAmount" Aggregate="@GridAggregateType.Sum" />
                            <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
                        </GridAggregates>
                        <GridColumns>
                                <GridColumn Width="100px">
                                <Template>
                                    @{
                                        var ctx = context as OrderItem;
                                        @if (ctx != null)
                                        {
                                             <MudButtonGroup Color="Color.Primary" Variant="Variant.Text">
                                                    <MudIconButton Icon="@Icons.TwoTone.ModeEdit" OnClick="@(()=>ServiceEditHandler(ctx))" />
                                                    <MudIconButton Icon="@Icons.TwoTone.Delete" OnClick="@(()=>ServiceDeleteHandler(ctx))" />
                                                    
                                             </MudButtonGroup>
                                            
                                        }

                                    }
                                </Template>
                            </GridColumn>
                            <GridColumn Field="TransactionItem.ItemName" Title="Service" Width="250px" />
                            <GridColumn Field="ResourceAddress.AddressName" Title="Technician" Width="250px" />
                            <GridColumn Field="TransactionUnit" Title="Unit" Width="100px" />
                            <GridColumn Field="TransactionQuantity" Title="Time" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                            <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                            <GridColumn Field="DiscountPercentage" Title="Dis%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                            <GridColumn Field="DiscountAmount" Title="Dis Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>

                                    <GridColumn Field="PrinciplePrecentage" Title="Princ%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))" />
                                    <GridColumn Field="PrincipleAmount" Title="Princ Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                            <GridColumn Field="CompanyPrecentage" Title="CM%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))"/>
                                    <GridColumn Field="CompanyAmount" Title="CM Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                <FooterTemplate>

                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                                    <GridColumn Field="CustomerAmount" Title="Cus Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" Visible="@(DataObject.OrderCategory1.Code.Equals("Good Will Warranty"))">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                                    <GridColumn Field="SubTotal" Title="Sub Total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                <FooterTemplate>
                                    <div style="text-align:right;">
                                        <span>@context.Sum?.ToString("N2")</span>
                                    </div>

                                </FooterTemplate>
                            </GridColumn>
                           
                        </GridColumns>
                    </TelerikGrid>
                }

            </div>

           
            </MudTabPanel>
            <MudTabPanel Text="Other" >
                
                    <div class="text-center mt-3">
                        <h5>ADD OTHER</h5>
                        <hr class="mt-2 mb-3" />
                    </div>
                    <div class="d-flex ma-3">
                        <UIRenderere FromSection="@AddOtherSection"
                                     DataObject="other"
                                     InteractionLogics="InteractionLogic"
                                     ObjectHelpers="ObjectHelpers" />
                    </div>
                    @if (DataObject.OtherServices != null)
                    {
                        <TelerikGrid Data=@DataObject.OtherServices.Where(x=>x.IsActive==1).ToList()
                                 FilterMode="@GridFilterMode.FilterMenu"
                                 Pageable="true"
                                 Sortable=true
                                 PageSize="5"
                                 @ref=GridRef3>

                            <GridAggregates>

                                <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                                <GridAggregate Field="ItemTaxType1" Aggregate="@GridAggregateType.Sum" />
                                <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
                            </GridAggregates>
                            <GridColumns>
                            <GridColumn Width="100px">
                                <Template>
                                    @{
                                        var ctx = context as OrderItem;
                                        @if (ctx != null)
                                        {
                                            <MudButtonGroup Color="Color.Primary" Variant="Variant.Text">
                                                <MudIconButton Icon="@Icons.TwoTone.ModeEdit" OnClick="@(()=>MiscEditHandler(ctx))" />
                                                <MudIconButton Icon="@Icons.TwoTone.Delete" OnClick="@(()=>MiscDeleteHandler(ctx))" />

                                            </MudButtonGroup>

                                        }

                                    }
                                </Template>
                            </GridColumn>
                                <GridColumn Field="TransactionItem.ItemName" Title="Service" Width="250px" />
                                <GridColumn Field="TransactionUnit.UnitName" Title="Unit" Width="100px" />
                                <GridColumn Field="Description" Title="Description" Width="250px" />

                                <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                    <FooterTemplate>
                                        <div style="text-align:right;">
                                            <span>@context.Sum?.ToString("N2")</span>
                                        </div>

                                    </FooterTemplate>
                                </GridColumn>

                                <GridColumn Field="ItemTaxType1" Title="VAT" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                    <FooterTemplate>
                                        <div style="text-align:right;">
                                            <span>@context.Sum?.ToString("N2")</span>
                                        </div>

                                    </FooterTemplate>
                                </GridColumn>

                                <GridColumn Field="SubTotal" Title="Sub Total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                                    <FooterTemplate>
                                        <div style="text-align:right;">
                                            <span>@context.Sum?.ToString("N2")</span>
                                        </div>

                                    </FooterTemplate>
                                </GridColumn>
                                
                            </GridColumns>
                        </TelerikGrid>
                    }
              
            
            </MudTabPanel>
    
        </MudTabs>
    </div>

</div>

</div>

<div class="col-12 d-flex justify-content-center gap-3 footer-proceed-add-other-btn">
    <MudButton Class="primary-btn mt-1" OnClick="OnGenerateMaterialRequisition" Disabled="IsButtonDisabled"> Generate Material Requisition </MudButton>
    <MudButton Class="primary-btn mt-1" OnClick="OnProceedToJobSummery" Disabled="IsButtonDisabled"> Save Detail</MudButton>
</div>


<MudMessageBox @ref="mbox" Title="Warning" >
    <MessageContent>
       please save the details before navigation!
    </MessageContent>
    <YesButton>
        <MudButton Variant="Variant.Filled" Color="Color.Error" >OK</MudButton>
    </YesButton>
</MudMessageBox>


<style>
    .footer-proceed-add-other-btn{
        position: fixed;
        width: 100%;
        left: 0;
        right: 0;
        bottom: 0;
        padding: 12px;
        background: white;
        height: fit-content;
        z-index: 3000;
    }

    .mat-ser-other-tab .mud-tabs-toolbar-wrapper{
        background:#E1E7EE;
    }

    .mat-ser-other-tab .mud-tab.mud-ripple {
        border-right:2px solid #053B71;
    }
</style>



@*<AddMaterialPopUp  OnCloseButtonClick="HideAllPopups"
                   WindowIsVisible="isAddMarterialPopUpShown"
                   DataObject="@DataObject"
                  PopUI="MaterialPopUp"
                  ObjectKey="@UIScope.ElementKey"
                  ItemAddedSuccessfull="RefreshMaterialGrid" />*@

@*<AddServicePopUp OnCloseButtonClick="HideAllPopups"
                  WindowIsVisible="isAddServicePopUpShown"
                  DataObject="@DataObject"
                 PopUI="ServicePopUp"
                  ObjectKey="@UIScope.ElementKey"
                  ItemAddedSuccessfull="RefreshServicesGrid" />*@

@*<CarMaterialGridEditPopUp PopUI="@MaterialPopUp"
                          UpdateItem="MaterialUpdateHandler"
                          ElementKey="@UIScope.ElementKey" @ref=matGridRef />*@

@*<div id="job-material-service-section2" class="col-12 mt-3">
    <h5>MATERIALS</h5>
    <div class="d-flex justify-content-end">
    <MudButton Class="primary-btn mt-1 mb-2" OnClick="@(()=>{isAddMarterialPopUpShown=true;})" Disabled="@IsButtonDisabled"> Add Materials </MudButton>
    </div>

    <UIRenderere FromSection="@MaterialPopUp"
                 DataObject="@DataObject"
                 InteractionLogics="@InteractionLogic"
                 ObjectHelpers="@ObjectHelpers" />
    <div class="mt-3">
        @if (DataObject.WorkOrderMaterials != null)
        {
            <TelerikGrid Data=@DataObject.WorkOrderMaterials.Where(x=>x.IsActive==1 && !x.TransactionItem.ItemName.Equals("-")).ToList()
                     FilterMode="@GridFilterMode.FilterMenu"
                     Pageable="true"
                     PageSize="3"
                     @ref=GridRef1>
                <GridToolBar>
                    <div class="d-flex align-items-center">
                        <span class="font-weight-bold">Materials</span>
                    </div>
                </GridToolBar>
                <GridAggregates>
                    <GridAggregate Field="TransactionQuantity" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="DiscountAmount" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="PrincipleAmount" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="CompanyAmount" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="CustomerAmount" Aggregate="@GridAggregateType.Sum" />
                    <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
                </GridAggregates>
                <GridColumns>
                    
                    <GridColumn Field="TransactionItem.ItemName" Title="Material" Width="250px" />
                    <GridColumn Field="TransactionUnit" Title="Unit" Width="100px" />
                    <GridColumn Field="TransactionQuantity" Title="Qty" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Field="DiscountPercentage" Title="Dis%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                    <GridColumn Field="DiscountAmount" Title="Dis Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>

                    <GridColumn Field="PrinciplePrecentage" Title="Princ%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                    <GridColumn Field="PrincipleAmount" Title="Princ Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Field="CompanyPrecentage" Title="CM%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                    <GridColumn Field="CompanyAmount" Title="CM Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Field="CustomerAmount" Title="Cus Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Field="SubTotal" Title="Sub total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                        <FooterTemplate>
                            <div style="text-align:right;">
                                <span>@context.Sum?.ToString("N2")</span>
                            </div>

                        </FooterTemplate>
                    </GridColumn>
                    <GridColumn Width="200px">
                        <Template>
                            @{
                                var ctx = context as OrderItem;
                                @if (ctx != null)
                                {
                                    <div class="d-flex gap-3">
                                        <MudIconButton Icon="@Icons.TwoTone.ModeEdit" OnClick="@(()=>MaterialEditHandler(ctx))" />
                                        <MudIconButton Icon="@Icons.TwoTone.Delete" OnClick="@(()=>MaterialDeleteHandler(ctx))" />
                                    </div>
                                }

                            }
                        </Template>


                    </GridColumn>
                </GridColumns>
            </TelerikGrid>
        }

    </div>
    <div class="mt-3">
        <h5>SERVICES</h5>
         <div class="d-flex justify-content-end">
        <MudButton Class="primary-btn mt-1 mb-2" OnClick="@(()=>{isAddServicePopUpShown=true;})" Disabled="@IsButtonDisabled"> Add Services </MudButton>
        </div>

        <UIRenderere FromSection="@ServicePopUp"
                     DataObject="@DataObject"
                     InteractionLogics="@InteractionLogic"
                     ObjectHelpers="@ObjectHelpers" />

        <div>
            @if (DataObject.WorkOrderServices != null)
            {
                <TelerikGrid Data=@DataObject.WorkOrderServices.Where(x=>x.IsActive==1 && !x.TransactionItem.ItemName.Equals("-")).ToList()
                         FilterMode="@GridFilterMode.FilterMenu"
                         PageSize="3"
                         Pageable="true">
                    <GridToolBar>
                        <div class="d-flex align-items-center">
                            <span class="font-weight-bold">Services</span>
                        </div>
                    </GridToolBar>
                    <GridAggregates>
                        <GridAggregate Field="Time" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="DiscountAmount" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="PrincipleAmount" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="CompanyAmount" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="CustomerAmount" Aggregate="@GridAggregateType.Sum" />
                        <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
                    </GridAggregates>
                    <GridColumns>
                        <GridColumn Field="TransactionItem.ItemName" Title="Service" Width="250px" />
                        <GridColumn Field="Address2.AddressName" Title="Technician" Width="250px" />
                        <GridColumn Field="TransactionUnit" Title="Unit" Width="100px" />
                        <GridColumn Field="Time" Title="Time" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Field="DiscountPercentage" Title="Dis%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                        <GridColumn Field="DiscountAmount" Title="Dis Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>

                        <GridColumn Field="PrinciplePrecentage" Title="Princ%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                        <GridColumn Field="PrincipleAmount" Title="Princ Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Field="CompanyPrecentage" Title="CM%" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px" />
                        <GridColumn Field="CompanyAmount" Title="CM Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>

                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Field="CustomerAmount" Title="Cus Amt" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Field="SubTotal" Title="Sub total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                            <FooterTemplate>
                                <div style="text-align:right;">
                                    <span>@context.Sum?.ToString("N2")</span>
                                </div>

                            </FooterTemplate>
                        </GridColumn>
                        <GridColumn Width="100px">
                            <Template>
                                @{
                                    var ctx = context as OrderItem;
                                    @if (ctx != null)
                                    {
                                        <div class="d-flex gap-3">
                                            <MudIconButton Icon="@Icons.TwoTone.ModeEdit" OnClick="@(()=>ServiceEditHandler(ctx))" />
                                            <MudIconButton Icon="@Icons.TwoTone.Delete" OnClick="@(()=>ServiceDeleteHandler(ctx))" />
                                        </div>
                                    }

                                }
                            </Template>
                        </GridColumn>
                    </GridColumns>
                </TelerikGrid>
            }

        </div>
        <div class="d-flex justify-content-end">
            <MudButton Class="primary-btn mt-1" OnClick="ShowAddOther"> Add Misc Services </MudButton>
        </div>
        <div class="d-flex justify-content-center gap-3">
            <MudButton Class="primary-btn mt-1" OnClick="OnGenerateMaterialRequisition"> Generate Material Requisition </MudButton>
            <MudButton Class="primary-btn mt-1" OnClick="OnProceedToJobSummery"> Save Detail</MudButton>
        </div>
    </div>
</div>

<div id="job-material-service-section3" class="col-12 mt-3 d-none">
    <div class="text-center">
        <h5>ADD OTHER</h5>
        <hr class="mt-2 mb-3" />
    </div>
    <UIRenderere FromSection="@AddOtherSection"
                 DataObject="other"
                 InteractionLogics="InteractionLogic"
                 ObjectHelpers="ObjectHelpers" />

    @if (DataObject.OtherServices != null)
    {
        <TelerikGrid Data=@DataObject.OtherServices.Where(x=>x.IsActive==1).ToList()
                 EditMode="@GridEditMode.Incell"
                 FilterMode="@GridFilterMode.FilterMenu"
                 OnUpdate="@MiscUpdateHandler"
                 OnEdit="@MiscEditHandler"
                 OnDelete="@MiscDeleteHandler"
                 Pageable="true"
                 Sortable=true
                 PageSize="5"
                 @ref=GridRef3>

            <GridAggregates>

                <GridAggregate Field="TransactionRate" Aggregate="@GridAggregateType.Sum" />
                <GridAggregate Field="ItemTaxType1" Aggregate="@GridAggregateType.Sum" />
                <GridAggregate Field="SubTotal" Aggregate="@GridAggregateType.Sum" />
            </GridAggregates>
            <GridColumns>
                <GridColumn Field="TransactionItem.ItemName" Title="Service" Width="250px" />
                <GridColumn Field="TransactionUnit.UnitName" Title="Unit" Width="100px" />
                <GridColumn Field="Description" Title="Description" Width="250px" />

                <GridColumn Field="TransactionRate" Title="Rate" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                    <FooterTemplate>
                        <div style="text-align:right;">
                            <span>@context.Sum?.ToString("N2")</span>
                        </div>

                    </FooterTemplate>
                </GridColumn>

                <GridColumn Field="ItemTaxType1" Title="VAT" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                    <FooterTemplate>
                        <div style="text-align:right;">
                            <span>@context.Sum?.ToString("N2")</span>
                        </div>

                    </FooterTemplate>
                </GridColumn>

                <GridColumn Field="SubTotal" Title="Sub total" DisplayFormat="{0:N2}" TextAlign="ColumnTextAlign.Right" Width="100px">
                    <FooterTemplate>
                        <div style="text-align:right;">
                            <span>@context.Sum?.ToString("N2")</span>
                        </div>

                    </FooterTemplate>
                </GridColumn>
                <GridCommandColumn Width="100px">
                    <GridCommandButton Command="Delete" Icon="delete">Delete</GridCommandButton>
                </GridCommandColumn>
            </GridColumns>
        </TelerikGrid>
    }
    <div class="d-flex justify-content-center footer-proceed-add-other-btn">
        <MudButton OnClick="OnBack" Color="Color.Primary" Variant="Variant.Filled">Proceed</MudButton>
    </div>
</div>*@